- read .github directory in the root directory first to see the obligations about how models communicate with each other (Workflow.md). 
- activate .venv  on linux or .venvmac
- source ~/.bash_profile on macos only
- use the .github/ directory to see how agents should talk to each other and what is the philosophy we have
- Remember Plan first, and stop for me to review the plan before proceeding
- Remember: Document before coding. 
- Remember: put tests that fail before coding.
- Remember: run the tests/ using pytest to make sure the regression works before the coding and after 
- 

# Read to understand 
1. Read some *.c and *.h files in the directory please to understand the structure. 
2. Read README.md in op_reporter/ 

# Actions to make 
1. I want you to add a CSV utility to write the same kind of files but the operations to be the x-axis (header), and the benchmarks on the y-axis (records of the csv). This is for both the single files and the master file. 
2. 